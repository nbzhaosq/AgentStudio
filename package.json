{
  "name": "agent-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @agent-studio/server dev",
    "dev:web": "pnpm --filter @agent-studio/web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @agent-studio/server start",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
